import Image from "next/image";

export default function Home() {
  return (
   <main className="p-10 flex items-center justify-center w-screen min-h-screen">
      {/* this is the services page where we list all the available services */}
   </main>
  );
}
